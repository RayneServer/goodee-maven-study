<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coma.study.account.AccountDAO">
	<insert id="insertAccount" parameterType="AccountDTO">
		INSERT INTO account (account_num, member_id, product_num)
		VALUES (#{accountNum}, #{memberId}, #{productNum})
	</insert>
	
	<insert id="insertAccountList" parameterType="java.util.List">
		INSERT INTO account (account_num, member_id, product_num)
		VALUES
		<foreach collection="list" item="account" separator=",">
			(#{account.accountNum}, #{account.memberId}, #{account.productNum})
		</foreach>
	</insert>
</mapper>